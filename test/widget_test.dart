// test/widget_test.dart
// Basic smoke test placeholder — app requires DB init so full widget test
// uses the unit tests in habit_card_test.dart and today_provider_test.dart.
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder test', () {
    expect(1 + 1, 2);
  });
}
